export const server=import.meta.env.VITE_SERVER;

//fix alertlistener, add refetch listener;
//something went wrong when accepting friend request;
//no user info after login;
//add loading and disable the register button;

//to-do: add feature to delete message on right click;
//add feature to make admin on click or right click on user in group;